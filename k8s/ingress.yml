apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ts-react-client-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - example.com
      secretName: vite-react-app-tls
  rules:
    - host: example.com
      http:
        paths:
          # 👇 ADD THIS NEW RULE FOR THE API GATEWAY 👇
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: ts-api-gateway # The name of your api-gateway service
                port:
                  number: 8080
          # 👇 YOUR EXISTING RULE FOR THE FRONTEND 👇
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ts-react-client-service
                port:
                  number: 5173